let bgColor = 'gray'
let interval

function setup() {
    createCanvas(600,400)
    fill(0)
    textAlign(CENTER,CENTER)
    interval = setInterval(changeBg, 1000)
}
 function draw() {
    background(bgcolor)
    let s = 59 - second
 }